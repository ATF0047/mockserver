language: java
on: [push]

jobs:
 check-bats-version:
  runs-on: ubuntu-latest
  steps:
   - run: mvn test-compile -pl mockserver-core -am
   - run: mvn -pl mockserver-core test -Dtest=org.mockserver.mock.HttpStateTest#shouldRetrieveLogEntriesWithRequestMatcher
